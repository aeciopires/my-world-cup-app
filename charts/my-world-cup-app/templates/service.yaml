apiVersion: v1
kind: Service
metadata:
  name: {{ include "my-world-cup-app.fullname" . }}
  labels:
    {{- include "my-world-cup-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "my-world-cup-app.selectorLabels" . | nindent 4 }}
